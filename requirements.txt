# ─────────────────────────────────────────────
#  AI Support Automation — Python Dependencies
#  Python 3.11+ compatible (FAISS removed)
# ─────────────────────────────────────────────

# Core Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
httpx==0.27.0

# LangChain
langchain==0.2.1
langchain-community==0.2.1

# Vector Store (replaces faiss-cpu — pure-Python, no compilation needed)
chromadb==0.5.3

# Embeddings (torch pulled in by sentence-transformers, no explicit pin = lighter CPU build)
sentence-transformers==3.0.1

# Data Processing
pandas==2.2.2
openpyxl==3.1.3
numpy==1.26.4

# Config & Env
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.3.0

# Logging
loguru==0.7.2

# Utils
tenacity==8.3.0
rich==13.7.1

# Testing
pytest==8.2.1
pytest-asyncio==0.23.7
